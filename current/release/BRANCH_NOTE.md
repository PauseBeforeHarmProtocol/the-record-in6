# Review branch note

This branch is an additive release overlay for `PauseBeforeHarmProtocol/the-record`.

Merge without deleting the preserved legacy archive:

- `the-record.html`
- `entries_array.js`
- `THE-RECORD-COMPLETE.pdf`
- `docs/`

The new root `index.html` is a navigable current-release front door. Historical source totals remain intentionally unresolved until the repository computes one authoritative count during build and CI.
